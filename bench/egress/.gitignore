# Bench output files. Regenerated by `make bench-egress`. Includes machine
# paths (binary_path, /home/USER/...) that should never land in the public repo.
results.json
sample-results.json
*.results.json
